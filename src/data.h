#include <stdio.h>
#include <opencv2/opencv.hpp>
#include <iostream>

using namespace cv;

class Data {
public:
    void Data(string);
    void getImage();
    double coinSumUp()
    Mat image;
    vector<Mat*> binary;
    coin[] piggyBank;
    
};

void Data::Data(string);

void Data::getImage();

coin[] fillPiggyBank(vector<Mat*>);